langchain>=0.2.0
langchain-openai>=0.1.0
langchain-core>=0.2.0
langgraph>=0.1.0
openai>=1.0.0
langchain-anthropic>=0.2.0
anthropic>=0.34.0
chromadb>=0.5.0
# sentence-transformers pulls in torch, which fails to unpack on Windows due to
# the 260-char path limit. It is optional: core/memory.py falls back to an
# in-memory store and ChromaDB uses onnxruntime by default. Install manually if
# needed after enabling Windows long paths: pip install sentence-transformers
# sentence-transformers>=2.0.0
pandas>=2.0.0
pyarrow>=14.0.0
fastparquet>=2023.0.0
duckdb>=0.10.0
plotly>=5.20.0
streamlit>=1.35.0
python-dotenv>=1.0.0
pendulum>=3.0.0
pytest>=8.0.0
pytest-cov>=5.0.0
